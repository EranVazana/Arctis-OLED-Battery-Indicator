import { File, FileMap } from './file';
import { ResolveDepOptions } from './options';
export declare class Loader {
    private options;
    private pool;
    private starting;
    private ended;
    private workerOptions;
    private size;
    private currentWorker;
    private initializing;
    constructor(options: ResolveDepOptions);
    setup(): Promise<undefined>;
    quit(): void;
    private getWorker;
    loadEntry(workingDirectory: string, request: string, files?: FileMap, warnings?: never[]): Promise<{
        entry: File | null;
        files: FileMap;
        warnings: never[];
    }>;
    private load;
}
